{% extends '/layouts/template.html.twig' %}
{% block title %}Category List{% endblock %}
{% block   setting %}{% endblock %}
{% block   selfCss  %}
        <link href="{{asset('css/common/btnstyle.css')}}" rel="stylesheet" />
        <link href="{{asset('css/common/table.css')}}" rel="stylesheet" />

{% endblock %}
{% block theContentPage %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
 <h1>Listes des Categories</h1>
<div class="wrapper-bouton-new d-flex justify-content-end">

<a href="{{ path('app_category_new') }}" class="my-btn">Nouveau</a>
</div>
<div class="wrapper-form-add d-flex justify-content-center">

 {{ include('admin_momo/category/_form.html.twig') }}
</div>
    <table class="table">
        <thead>
            <tr>
                {# <th>Id</th> #}
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                {# <td>{{ category.id }}</td> #}
                <td>{{ category.libelle }}</td>
                <td>
                    <a href="{{ path('app_category_show', {'id': category.id}) }}">Détails</a>
                    <a href="{{ path('app_category_edit', {'id': category.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
</div>
   
{% endblock %}
